---
- name: Deploy and tune PostgreSQL on dedicated VM
  hosts: postgres_vm
  gather_facts: true
  become: true
  vars:
    # Set these secure values before running in real environment
    pg_admin_user: "postgres"
    pg_admin_password: "CHANGE_ME_SECURE_PASSWORD"

  roles:
    - role: pg_install
