apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: result
spec:
  type: LoadBalancer
  selector:
    app: result
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
