apiVersion: v1
kind: Service
metadata:
  name: vote
  labels:
    app: vote
spec:
  type: NodePort
  selector:
    app: vote
  ports:
    - name: vote
      port: 5000
      targetPort: 5000
      nodePort: 31000
      protocol: TCP
