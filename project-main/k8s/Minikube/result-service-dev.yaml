apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: result
spec:
  type: NodePort
  selector:
    app: result
  ports:
    - name: result
      port: 4000
      targetPort: 4000
      nodePort: 31001
      protocol: TCP
